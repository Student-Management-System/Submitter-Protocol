/**
 * Base package of the protocol, which is divided into two sub packages.
 * <ul>
 *   <li><tt>backend</tt>: Contains a low level API</li>
 *   <li><tt>frontend</tt>: Contains a high level API composed of low-level classes to be used directly inside the 
 *       submitters and reviewer.</li>
 * </ul>
 * 
 */
package net.ssehub.exercisesubmitter.protocol;